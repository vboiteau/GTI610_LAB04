apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'application'

mainClassName = 'com.TCP.ServerTCP'
// tag::jar[]
jar {
    archivesBaseName = 'TCP'
	    version =  '0.1.0'
}
// end::jar[]
sourceCompatibility = 1.8
targetCompatibility = 1.8

run {
 if (project.hasProperty("appArgs")) {
    args Eval.me(appArgs)
 }
}

// tag::wrapper[]
// end::wrapper[]